#functions
functions are programs that always generate some output it maybe even true or false
theory
Functions can input parameters/arguments and optional return values using return statement 
There is user defined and inbuilt functions to be used while you code
code:
def computepay(hours, rate):
    if hours <= 40:
        pay = hours * rate
    else:
        regular_pay = 40 * rate
        overtime_hours = hours - 40
        overtime_pay = overtime_hours * (rate * 1.5)
        pay = regular_pay + overtime_pay
    return pay

# Prompt user for input
hours_str = input("Enter Hours: ")
rate_str = input("Enter Rate: ")

# Convert string inputs to floating-point numbers
hours = float(hours_str)
rate = float(rate_str)

# Call the function and store the returned value
total_pay = computepay(hours, rate)

# Display the result
print("Pay", total_pay)
concept:
Understanding function parameters , return values and calling arguements
